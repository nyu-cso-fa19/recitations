#include <stdio.h>

unsigned int string_length(char *s) {
	//TODO: Write code that returns the length of string s here

}

int main(int argc, char **argv) {
	unsigned int answer = 0;
	/*
	 * TODO: Write code that computes the sum of the length of all strings except the first in argv here
	 * That means it should ignore the 0th element of argv, which is the program name
	 *
	 * So, if I run ./prob1 abcdefghijklmnopqrstuvwxyz
	 * 'answer' should equal 26, not 33 or any other number
	 * because it should not count './prob1', and 'abcdefghijklmnopqrstuvwxyz'
	 * has 26 characters in it
	 *
	 */




	//DO NOT DELETE THE LINE BELOW
	printf("%u\n", answer);
}
